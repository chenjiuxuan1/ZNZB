id: operation.ab_experiment_effect
kind: metric
name: AB experiment effect
description: Example metric contract only; it is not a production semantic seed.
owner:
  name: owenzhang
  team: data-warehouse
freshness:
  expected_schedule: daily
  max_age_hours: 48
  verified_at: null
source:
  tier: semantic
  uri: semantic-layer/metrics/operation.ab_experiment_effect.yaml
canonical_assets:
  - example_db.example_table
filters:
  - name: exclude_test_account
    expression: is_test_account = 0
validation:
  status: draft
  notes:
    - Contract example only; KNOW-02 owns real semantic seeds.
